{
  "name": "@beamos/openapi-client",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "bun run scripts/generate-openapi-client.mjs",
    "build": "rm -rf src/generated/* && bun run generate && tsc -p tsconfig.json"
  },
  "dependencies": {
    "openapi-fetch": "^0.15.0"
  }
}
