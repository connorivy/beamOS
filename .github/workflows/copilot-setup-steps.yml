name: Copilot Setup Steps

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.9"
      - name: Install dependencies (api)
        working-directory: apps/api
        run: bun install
      - name: Install dependencies (web)
        working-directory: apps/web
        run: bun install
      - name: Install dependencies (openapi-client)
        working-directory: packages/openapi-client
        run: bun install
