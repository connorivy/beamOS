// Mock handlers for API calls
export const mockHandlers = [
  // Add MSW handlers here for API mocking
  // Example:
  // rest.get('/api/users', (req, res, ctx) => {
  //   return res(ctx.json([{ id: 1, name: 'John Doe' }]))
  // })
]